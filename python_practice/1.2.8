#!usr/bin/env python
#coding: utf-8


def find_different(src):
    res_cnt = [0 for i in range(src[-1])]
    for src_idx in range(len(src)):
        src_value = src[src_idx]
        res_cnt[src_value-src[0]]  += 1
    res = 0 
    for num in res_cnt:
        if num == 0:
            res += 1

    return res



def compare_list(src1, src2):
    for i in src1:
        for j in src2:
            if src1[i] != src2[j]
                return False

if __name__ == '__main__':
    assert compare_list([1,3,4,6]
